const user = {
    name : "sarath",
    age : 22,
    location : "nellore"
}

// user is the object (keys , value)

// unwrap it

const {name , age , location} = user; 

console.log(name)
console.log(age);
console.log(location);